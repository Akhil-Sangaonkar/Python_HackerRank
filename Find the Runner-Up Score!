if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())
    arr = sorted(arr,reverse=True)
    for i in arr:
        answer = max(arr)-i
        if (answer !=0):
            print(i)
            break
    
